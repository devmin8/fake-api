meta {
  name: Feed Unauthed
  type: http
  seq: 42
}

get {
  url: {{baseUrl}}/api/feed
  body: none
  auth: none
}

script:pre-request {
  req.setHeader('Cookie', 'x=1');
}

assert {
  res.status: eq 401
  res.body.error.code: eq UNAUTHENTICATED
}
