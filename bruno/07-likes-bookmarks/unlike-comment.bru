meta {
  name: Unlike Comment
  type: http
  seq: 100
}

delete {
  url: {{baseUrl}}/api/comments/{{likeCommentId}}/like
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.liked: eq false
  res.body.likeCount: eq 0
}
