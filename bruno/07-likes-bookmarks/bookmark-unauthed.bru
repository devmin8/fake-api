meta {
  name: Bookmark Unauthed
  type: http
  seq: 160
}

post {
  url: {{baseUrl}}/api/posts/{{likePostId}}/bookmark
  body: none
  auth: none
}

script:pre-request {
  req.setHeader('Cookie', 'x=1');
}

assert {
  res.status: eq 401
  res.body.error.code: eq UNAUTHENTICATED
}
